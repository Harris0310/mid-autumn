# AGENTS.md · 中秋祝福页

给这个仓库干活的约定。**开工前先读 `交接说明.md`**（本地文档、不入库）：
那里有环境、踩过的坑、用户已经敲定的选择，和下一步该做什么。

## 一次改动算完成，当且仅当这四件事都做完

1. `node _dev/check.js` 全过。
2. `node _dev/browsercheck.js` 跑完，并且**逐张看过 `_dev/` 里新生成的截图**。
   断言全绿而画面是错的很常见：这个项目两次真 bug（信被画到左上角半尺寸、
   信息流挤在左半边）都是断言全过、只有截图能看出来。
3. `git commit`。
4. **`git push origin HEAD`。** 用户要求每次改完就推上 GitHub，不用等他问。
   推送走系统 ssh（`core.sshCommand` 已经配好）。

## 约定

- `js/config.js` 是**唯一配置入口**：文案、速度、密度、信的内容都在那里。
  调观感改它，实现文件里的魔数保持不动。
- 观感参数（速度、密度、字号、配色、节奏）先问用户再动，动完把新截图发给他确认
  —— 速度这条他已经来回改过两次。
- 配置注释里写了"别随手删"的，是实测调出来的性能机关（字形缓存、发光范围、
  渐变复用、信纸位图、信升到位后跳过爱心）。
- 收工回复里报**实测**数字（帧时中位/p90、速度 px/s），不要照着代码推。
